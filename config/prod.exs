import Config

# Minimal production config file.
# Keep compile-time production configuration here. Runtime configuration
# (secrets, database URL, etc.) is handled in `config/runtime.exs`.

# Example compile-time endpoint config (uncomment and adjust if needed):
# config :yt_tracker, YtTrackerWeb.Endpoint,
#   http: [port: 4000],
#   server: true
